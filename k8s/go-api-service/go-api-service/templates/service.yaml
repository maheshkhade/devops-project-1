apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-api-service.fullname" . }}
  labels:
    {{- include "go-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-api-service.selectorLabels" . | nindent 4 }}
