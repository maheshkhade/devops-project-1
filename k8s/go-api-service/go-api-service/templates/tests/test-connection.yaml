apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-api-service.fullname" . }}-test-connection"
  labels:
    {{- include "go-api-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-api-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
